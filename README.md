# Server-in-C

Cognitive Arena 2026 is a local browser-based cognitive trainer built in C + Winsock. It serves a modern dark UI with 6 games plus a dashboard that updates live and tracks your best + recent results. Run the server and open `http://127.0.0.1:8080/`. Scores are saved to `metrics.txt` (delete it to reset). 
