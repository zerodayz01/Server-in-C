# Server-in-C

A local browser-based cognitive trainer built in C. Includes 6 games plus a dashboard that updates live and tracks your best + recent results. Scores are saved to `metrics.txt` (delete it to reset). 
